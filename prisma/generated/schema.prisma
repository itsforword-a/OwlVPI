generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-arm64-openssl-1.0.x"]
  output        = "./prisma/generated" // <-- ДОБАВЬТЕ ЭТУ СТРОКУ
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
